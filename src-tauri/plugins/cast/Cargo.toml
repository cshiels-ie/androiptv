[package]
name = "tauri-plugin-cast"
version = "0.1.0"
description = "Native Chromecast (AndroidX Cast SDK) for the AndroIPTV player — Android only; a no-op on desktop"
edition = "2021"
rust-version = "1.78"
links = "tauri-plugin-cast"

[dependencies]
tauri = { version = "2", features = [] }
serde = "1"

[build-dependencies]
tauri-plugin = "2"
